/** Галерея фото работ отключена — компонент-заглушка для совместимости. */
export function WorkGallery(_props: { label: string; images?: readonly { src: string; alt: string }[] }) {
  return null;
}
